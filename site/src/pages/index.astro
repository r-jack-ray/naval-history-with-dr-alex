---
import BaseLayout from "../layouts/BaseLayout.astro";
import { archiveSegments, archiveTopics, archiveVideos } from "../data/archive";

const baseUrl = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const featuredVideo = archiveVideos[0];
const featuredVideoUrl = `${baseUrl}videos/${featuredVideo.slug}/`;
const featuredSegments = archiveSegments.slice(0, 4);
const activeTopics = [...archiveTopics]
  .sort((left, right) => right.segmentCount - left.segmentCount || right.videoCount - left.videoCount)
  .slice(0, 10);
---

<BaseLayout
  title="Naval History with Dr. Alex Archive"
  description="Browse Naval History with Dr. Alex videos by timestamp, subject, and Q&A."
  pageType="home"
>
  <main id="content" class="home-page" data-pagefind-body>
    <section class="intro">
      <div class="intro-copy">
        <p class="eyebrow">Naval History Archive</p>
        <h1>Dr. Alex Clarke videos, organized by timestamp and topic.</h1>
        <p class="lede">
          Browse channel videos through timestamped notes, source links, and topic pages for
          ships, battles, navies, operations, and Q&A.
        </p>
        <div class="action-row">
          <a class="primary-link" href={`${baseUrl}videos/`}>Browse videos</a>
          <a class="secondary-link" href={`${baseUrl}search/`}>Search archive</a>
        </div>
      </div>

      <a class="feature-card" href={featuredVideoUrl}>
        <img
          src={featuredVideo.thumbnailUrl}
          alt={`Thumbnail for ${featuredVideo.title}`}
          loading="lazy"
          width="480"
          height="360"
        />
        <div>
          <span>Featured video</span>
          <strong>{featuredVideo.title}</strong>
        </div>
      </a>
    </section>

    <section class="status-grid" aria-label="Archive sections">
      <article>
        <span class="label">Videos</span>
        <h2>{archiveVideos.length} videos</h2>
        <p>Each page groups the YouTube link, key details, topics, and related timestamps.</p>
      </article>
      <article>
        <span class="label">Segments</span>
        <h2>{archiveSegments.length} timestamped segments</h2>
        <p>Chapters, notable points, Q&A, and source passages stay grouped by timestamp.</p>
      </article>
      <article>
        <span class="label">Topics</span>
        <h2>{archiveTopics.length} browsable topics</h2>
        <p>Topic pages collect related videos and timestamped notes under stable subject labels.</p>
      </article>
    </section>

    <section class="home-section" aria-labelledby="featured-segments-heading">
      <div class="section-title-row">
        <div>
          <p class="eyebrow">Segments</p>
          <h2 id="featured-segments-heading">Featured timestamped notes</h2>
        </div>
        <a class="secondary-link" href={`${baseUrl}segments/`}>All segments</a>
      </div>
      <div class="record-grid compact-grid">
        {
          featuredSegments.map((segment) => (
            <article class="segment-card">
              <span class="label">{segment.kindLabel} · {segment.start}</span>
              <h3><a href={`${baseUrl}segments/${segment.slug}/`}>{segment.title}</a></h3>
              <p>{segment.summary}</p>
            </article>
          ))
        }
      </div>
    </section>

    <section class="home-section" aria-labelledby="active-topics-heading">
      <div class="section-title-row">
        <div>
          <p class="eyebrow">Topics</p>
          <h2 id="active-topics-heading">Active subject areas</h2>
        </div>
        <a class="secondary-link" href={`${baseUrl}topics/`}>All topics</a>
      </div>
      <div class="topic-cloud" aria-label="Active topics">
        {
          activeTopics.map((topic) => (
            <a href={`${baseUrl}topics/${topic.slug}/`}>
              <strong>{topic.title}</strong>
              <span>{topic.videoCount} videos · {topic.segmentCount} segments</span>
            </a>
          ))
        }
      </div>
    </section>
  </main>
</BaseLayout>
