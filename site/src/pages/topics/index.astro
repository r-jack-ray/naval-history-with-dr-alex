---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { archiveTopics } from "../../data/archive";

const baseUrl = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<BaseLayout
  title="Topics | Naval History with Dr. Alex Archive"
  description="Browse naval history topics, ships, battles, navies, and periods."
  pageType="topic-index"
>
  <main class="page-shell" data-pagefind-body>
    <section class="section-heading">
      <p class="eyebrow">Topics</p>
      <h1>Browse by subject.</h1>
      <p class="lede">
        Topic pages collect related videos and timestamped segment entries while the controlled
        topic list stays small and auditable.
      </p>
    </section>

    <section class="record-grid topics" aria-label="Topics">
      {
        archiveTopics.map((topic) => (
          <article class="topic-card">
            <span class="label">{topic.videoCount} videos · {topic.segmentCount} segments</span>
            <h2><a href={`${baseUrl}topics/${topic.slug}/`}>{topic.title}</a></h2>
            <p>{topic.summary}</p>
            {topic.aliases.length > 0 && <small>Also: {topic.aliases.join(", ")}</small>}
          </article>
        ))
      }
    </section>
  </main>
</BaseLayout>
