interface:
  display_name: "Naval Site Build Repair"
  short_description: "Diagnose and safely repair naval site builds"
  default_prompt: "Use $naval-site-build-repair to diagnose and safely repair the current site build failure without running a full site build unless I explicitly allow it."
